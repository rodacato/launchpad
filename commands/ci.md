---
description: "Run a launchpad CI skill: github"
---

Read `skills/$ARGUMENTS/SKILL.md` and follow its instructions exactly.

If `$ARGUMENTS` is empty or not one of: github — respond with:

```
Usage: /launchpad:ci <skill>

Available skills:
  github   Configure GitHub labels, workflows, issue templates, PR template
```
