---
description: "Find the right skill — meta-skill that routes the task across launchpad / lifecycle / philosophy and loads the inherited floor"
---

Read `skills/orient/SKILL.md` and follow its instructions exactly.

An optional task description can be provided in `$ARGUMENTS`. If present,
treat it as the task to classify and route. If empty, ask the human:
"What's the task you want to route?" and wait for the answer before
proceeding.

```
Usage: /philosophy:orient [task description]

Examples:
  /philosophy:orient
  /philosophy:orient set up a release process for a new repo
  /philosophy:orient should I use identity or voice for this?
  /philosophy:orient review PR 42
```
