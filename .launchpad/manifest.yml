# Launchpad Template Manifest
# Only tracks version. Sync is handled by a remote script.
# To leave launchpad: delete this entire .launchpad/ directory.

version: "0.4.0"
source: rodacato/launchpad
branch: master
